events {}

http {
    upstream web_backend {
        least_conn;

        {% for web in groups['web'] %}
            server {{ web }};
        {% endfor %}
    }

    server {
        listen 80;

        location / {
            proxy_pass http://web_backend;

        }
    }
}
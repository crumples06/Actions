global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets:
{% for host in groups['web'] + groups['loadBalancer'] + groups['db'] %}
          - '{{ host }}:9100'
{% endfor %}